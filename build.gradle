plugins {
    id 'java'
    id 'base' // clean task를 위해 추가
}

group = 'com.example'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // 추후 백엔드에 사용할 수 있는 예시 의존성
    // testImplementation 'org.junit.jupiter:junit-jupiter:5.8.1'
}

test {
    useJUnitPlatform()
}
